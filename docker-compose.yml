services:
  db:
    image: postgres:16
    container_name: postgres-prep1
    restart: "no"
    environment:
      POSTGRES_PASSWORD: senha
      POSTGRES_DB: prep1_db
    ports:
      - "5433:5432"
    volumes:
      - pg_prep1_data:/var/lib/postgresql/data

volumes:
  pg_prep1_data:
